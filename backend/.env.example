# MAI Launcher Backend 环境变量配置示例

# 服务器配置
HOST=127.0.0.1
PORT=11111

# API 配置
API_V1_PREFIX=/api/v1
PROJECT_NAME=MAI Launcher API
VERSION=0.1.0

# CORS 配置（多个源用逗号分隔）
ALLOWED_ORIGINS=http://localhost:5173,http://127.0.0.1:5173

# GitHub API Token (可选,用于提高 API 速率限制)
# 获取方式: https://github.com/settings/tokens
# 需要的权限: public_repo (读取公开仓库)
# 未配置时使用匿名访问,每小时限制 60 次请求
# 配置后每小时限制 5000 次请求
GITHUB_TOKEN=

# 存储路径
INSTANCES_DIR=./deployments

# 日志配置
LOG_LEVEL=INFO
