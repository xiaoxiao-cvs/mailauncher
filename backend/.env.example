# MAI Launcher Backend 环境变量配置示例

# 服务器配置
HOST=127.0.0.1
PORT=11111

# API 配置
API_V1_PREFIX=/api/v1
PROJECT_NAME=MAI Launcher API
VERSION=0.1.0

# CORS 配置（多个源用逗号分隔）
ALLOWED_ORIGINS=http://localhost:5173,http://127.0.0.1:5173

# GitHub API Token (强烈推荐配置,避免速率限制)
# 获取方式: https://github.com/settings/tokens/new
# 步骤:
#   1. 访问上述链接
#   2. Note 填写: MAI Launcher API Access
#   3. Expiration 选择: No expiration 或自定义
#   4. 勾选权限: public_repo (或只读的 repo scope)
#   5. 点击 Generate token
#   6. 复制生成的 token 粘贴到下方
# 速率限制对比:
#   - 未配置: 每小时 60 次请求 (很容易超限)
#   - 已配置: 每小时 5000 次请求
GITHUB_TOKEN=

# 存储路径
INSTANCES_DIR=./deployments

# 日志配置
LOG_LEVEL=INFO
