# Web 框架
fastapi==0.115.6
uvicorn[standard]==0.32.1
python-multipart==0.0.6

# 数据模型
pydantic==2.10.3
pydantic-settings==2.6.1

# 配置文件处理
toml==0.10.2
tomlkit>=0.12.0

# HTTP 客户端
httpx==0.27.2
aiohttp==3.11.11

# 数据库
sqlalchemy>=2.0.36  # 需要 2.0.36+ 支持 Python 3.13
aiosqlite==0.19.0
greenlet==3.2.4

# 日志
loguru==0.7.2

# 进程管理
pywinpty>=2.0.13; sys_platform == 'win32'  # Windows PTY 支持（使用 >= 允许安装预编译的新版本）
psutil==6.1.0  # 进程资源监控

# 任务调度
APScheduler==3.10.4

# 测试依赖
requests==2.31.0
pyinstaller>=6.10.0  # 支持 Python 3.13 的版本
